@model IEnumerable<NominasSAT.Models.SalarioMinimo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Monto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Monto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Zona }) |
            @Html.ActionLink("Details", "Details", new { id=item.Zona }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Zona })
        </td>
    </tr>
}

</table>
