@model IEnumerable<NominasSAT.Models.Periodo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Predeterminado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Predeterminado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PeriodoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PeriodoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PeriodoID })
        </td>
    </tr>
}

</table>
